# ============================================
# BACKEND - VARIABLES DE ENTORNO
# ============================================
# Copiar este archivo como .env y completar los valores

# --- BASE DE DATOS ---
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Ejemplo local: postgresql://postgres:password@localhost:5432/lubenergystore
# Ejemplo Dokploy: postgresql://postgres:password@postgres:5432/lubenergystore
DATABASE_URL="postgresql://usuario:contraseña@host:5432/lubenergystore"

# --- JWT (SEGURIDAD) ---
# IMPORTANTE: Generar un secreto seguro para producción
# Ejecutar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="CAMBIAR_POR_UN_SECRETO_SEGURO_DE_64_CARACTERES_MINIMO"

# --- CORS ---
# URL del frontend (sin barra final)
# Desarrollo: http://localhost:3000
# Producción: https://tudominio.com
FRONTEND_URL="https://tudominio.com"

# --- SERVIDOR ---
# Puerto del backend (Dokploy suele usar 3000 interno)
PORT=3080

# --- ENTORNO ---
# development | production
NODE_ENV="production"

# --- ZONA HORARIA (fechas en mails y referencia para el front) ---
# IANA, recomendado para Argentina:
APP_TIMEZONE="America/Argentina/Buenos_Aires"

# --- CONTACTO TIENDA (link wa.me en plantillas de email, solo dígitos sin +) ---
# Ej. 379 5056878 → 543795056878
STORE_WHATSAPP_WA_ME_ID="543795056878"

# --- NOTIFICACIONES AL DUEÑO (email: venta confirmada y abandono de pago 24h) ---
OWNER_NOTIFICATION_EMAIL="dueño@tudominio.com"

# --- EMAIL (Resend) ---
# Obtener en: https://resend.com/api-keys
RESEND_API_KEY="re_xxxxxxxxxx"
EMAIL_FROM="noreply@tudominio.com"

# --- TALO PAY (PLAYGROUND / SANDBOX) ---
# Credenciales sandbox de Talo
TALO_CLIENT_ID="tu_talo_client_id"
TALO_CLIENT_SECRET="tu_talo_client_secret"
TALO_USER_ID="tu_talo_user_id"

# sandbox | production (para playground usar sandbox)
TALO_ENVIRONMENT="sandbox"
 
# URL pública que recibirá webhooks de prueba.
# Ejemplo local con ngrok:
# https://abc123.ngrok-free.app/talo/webhook/test
TALO_WEBHOOK_URL="https://tu-url-publica/talo/webhook/test"

# URL base de retorno para checkout (fallback cuando frontend no envía redirect_url)
# Se le agrega automáticamente /{orderId}.
# También puedes usar placeholder explícito:
# https://tudominio.com/pedido/{orderId}
TALO_REDIRECT_URL="https://tudominio.com/pedido"

# Conversión de montos internos al formato Talo:
# - ars: usa el valor tal cual (recomendado si en tu catálogo 26000 = ARS 26000)
# - cents: divide por 100 (para sistemas que guardan centavos)
# - auto: infiere por rango de precios para compatibilidad temporal
TALO_AMOUNT_MODE="ars"

# ============================================
# NOTAS PARA DOKPLOY
# ============================================
# 1. En Dokploy, agregar estas variables en:
#    App Settings > Environment Variables
#
# 2. Si usas PostgreSQL de Dokploy:
#    - El host suele ser el nombre del servicio (ej: "postgres")
#    - El puerto interno es 5432
#
# 3. El FRONTEND_URL debe ser el dominio con HTTPS
#    que configuraste en Dokploy para el frontend
#
# 4. NODE_ENV=production activa:
#    - Cookies seguras (secure: true)
#    - Headers de seguridad con Helmet
# ============================================
